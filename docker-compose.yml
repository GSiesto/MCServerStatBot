services:
  mcserverstatbot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mcserverstatbot
    restart: unless-stopped
    env_file:
      - .env
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
